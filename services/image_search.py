class ImageSearchService:
    def search_images(self, query, language):
        # Fake list of image URLs (replace with real search API later)
        return [
            f"https://example.com/images?q={query}_1",
            f"https://example.com/images?q={query}_2"
        ]
